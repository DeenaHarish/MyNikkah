import {View, Text} from 'react-native';
import React from 'react';

const CustomeHeaderTitle = () => {
  return (
    <View>
      <Text style={{color: '#fff', fontWeight: 'bold'}}>Custom Header</Text>
    </View>
  );
};

export default CustomeHeaderTitle;
